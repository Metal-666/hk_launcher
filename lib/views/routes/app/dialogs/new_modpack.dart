import 'package:fluent_ui/fluent_ui.dart';

class NewModpackDialog extends StatelessWidget {
  const NewModpackDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => ContentDialog();
}
